"use strict";

/**
 * The Rock Island public appraisal source intentionally excludes owner and
 * tax-bill identity fields, so there is no owner mapping stage.
 *
 * @returns {void}
 */
function main() {}

main();
