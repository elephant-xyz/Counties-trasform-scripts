"use strict";

/**
 * The county FeatureServer exposes only aggregate building year and square
 * footage, which map to property fields rather than a detailed structure.
 *
 * @returns {void}
 */
function main() {}

main();
